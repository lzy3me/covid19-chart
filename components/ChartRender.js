import React from 'react'
import { HorizontalBar } from 'react-chartjs-2'
import { useChartContext } from '../context/ChartContext'

const ChartData = props => {
    return(
        <HorizontalBar data="" height={1000} />
    )
}